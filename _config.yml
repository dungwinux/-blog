# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: dungwinux's Blog
author: Nguyễn Tuấn Dũng
email: ntddebugger@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Dev blog and notes of some of my interests.
baseurl: "/-blog" # the subpath of your site, e.g. /blog
url: "https://dungwinux.github.io" # the base hostname & protocol for your site, e.g. http://example.com
github_username: dungwinux

# Build settings
theme: minima
# remote_theme: ronv/sidey

plugins:
  - jemoji
sass:
  style: compressed
markdown: kramdown
input: GFM
highlighter: rouge

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
